// You can leave this file empty OR delete it.
// Seeding helpers already exist inside app.js (seedWeeklyData / seedWeeklyIfEmpty).


// A tiny helper so admins can seed the weekly table right from the UI (called by the “Seed” button). If you prefer, you can keep this merged in app.js (it already contains seedWeeklyData() and seedWeeklyIfEmpty()), so this file is truly optional.